export const aritmetica = {
    sumar,
    restar,
    multiplicar
} ;
 
function sumar(a,b) {
    return a + b;
};
function restar(a,b) {
    return a - b;
};
function multiplicar(a,b) {
    return a * b;
}